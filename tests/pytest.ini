[pytest]
addopts = -ra -q
testpaths = tests
log_cli = true
log_cli_level = INFO
markers =
    smoke: quick smoke tests
    regression: full regression tests
    addition: addition test cases
    export: export-related tests
    filters: filter-related tests
    ui: ui validation tests
    performance: performance tests

